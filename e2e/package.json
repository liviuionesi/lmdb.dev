{
  "name": "lmdb-e2e",
  "version": "1.0.0",
  "description": "Playwright E2E tests driving the real LMDB React app against the local backend stack",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@types/node": "^20.14.9"
  }
}
