<!-- number: 26
     state: CLOSED
     labels: task, infrastructure
     milestone: Sprint 6
     parent: #88
     children: none -->
# [TASK] Terraform: Azure AKS free-tier infrastructure

## Task
Provision the primary Azure demo environment with Terraform (ARCHITECTURE.md §11.2): an AKS cluster (Free control-plane tier) with one small node, a network module, and a budget-guard module (a zero-spend Azure Cost Management budget + alert, replacing a paid container registry — images pull from ghcr.io instead). CI authenticates to Azure without a stored long-lived secret.

## Acceptance Criteria
- [x] `terraform apply` -> `kubectl apply -k overlays/azure` -> `terraform destroy` round-trip completed against a real Azure subscription, zero manual console steps (2026-07-29, commit `aa52b76`)
- [x] All Terraform resources tagged `project=filmpire`, `managed-by=terraform` (Azure Consumption Budgets don't support tags at all — the one exception)
- [x] CI authenticates via GitHub OIDC federated credentials — no stored `ARM_CLIENT_SECRET`
- [x] Gateway reachable end-to-end on the deployed cluster — real TMDB data flowing gateway -> movie-service -> MongoDB -> Redis -> gateway (re-verified 2026-08-01)
- [x] Cluster runs within Azure free credits, budget-guard tripwire live — `Standard_D2ls_v7` (the entire B-series family is blocked as anti-abuse on this subscription) is a small real hourly cost, covered by free credits — same "free" definition as #27

## Estimate
**Hours:** 15

## Notes
Second live-apply pass (2026-08-01) found and fixed 4 more bugs: missing `movie-service` `startupProbe`, AKS's auto-created per-node NSG blocking the gateway, an `upgrade_settings` drift bug forcing a spurious NSG replace on every plan, and IPv6-only DNS resolution hanging outbound JVM calls (fixed via `-Djava.net.preferIPv4Stack=true`). `terraform destroy` reconfirmed clean afterward. ARCHITECTURE.md corrected to match (v1.6.1).

Reference: ARCHITECTURE.md §11.1–11.2
