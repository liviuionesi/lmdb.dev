<!-- number: 260
     state: CLOSED
     labels: P2-medium, task
     milestone: Sprint 10
     parent: #271
     children: none -->
# [TASK] Audit Story #88 and Tasks #25-#27 (Kubernetes manifests and free-tier Azure/AWS IaC)

## Task
Audit story #88 and everything under it with `/audit-artifacts 88`:
shape, voice, truth, proof.

Subject: free-tier IaC for Azure and AWS with Kubernetes manifests

Scope: Story #88 and its 3 tasks.

## Acceptance Criteria
- [x] Task #25 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Task #26 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Task #27 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Story #88 audited: matches the user-story template, meets the writing
      standard, and its criteria reflect what the audited tasks actually prove
- [x] Metadata and linkage correct across story #88 and its tasks: one type
      label, one priority label, no `sprint-N` label, native parent link present

## Estimate
**Hours:** 4

## Notes
Parent story #271 covers epic #22 as a whole. The epic body
itself is audited there, not here.

Found and fixed real drift in #25, #26, #27, all traced to #151 (the full local-parity service set) and the project's rebrand to `lmdb.dev`: the Azure/AWS overlays now deploy 9 services, not the 4-service core slice #25 described; `Standard_D2ls_v7`/`t3.small` node sizes and the `project=filmpire` tag are stale, replaced by `Standard_D4ls_v7`/`m7i-flex.large` and `project=lmdb`; and #27's "OIDC not wired into CI" line is now false — `deploy.yml` uses `role-to-assume` with `id-token: write`. Every acceptance criterion across #25, #26, #27, #88 now names a command or, where the criterion is inherently live-infrastructure, is marked `(manual: ...)`.
