<!-- number: 257
     state: CLOSED
     labels: P2-medium, task
     milestone: Sprint 9
     parent: #270
     children: none -->
# [TASK] Audit Story #85 and Tasks #16-#18 (movie, user and actor catalog services)

## Task
Audit story #85 and everything under it with `/audit-artifacts 85`:
shape, voice, truth, proof.

Subject: movie, user, and actor catalog services with tested REST APIs

Scope: Story #85 and its 3 tasks.

## Acceptance Criteria
- [x] Task #16 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Task #17 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Task #18 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Story #85 audited: matches the user-story template, meets the writing
      standard, and its criteria reflect what the audited tasks actually prove
- [x] Metadata and linkage correct across story #85 and its tasks: one type
      label, one priority label, no `sprint-N` label, native parent link present
      (`audit-check.py 85 --tree`)

## Estimate
**Hours:** 4

## Notes
Parent story #270 covers epic #15 as a whole. The epic body
itself is audited there, not here.

Two criteria had no test and got one: `OpenApiDocsTest` in each of the three
services, and a newman request reading the Eureka registry, so "registered
with Eureka, OpenAPI docs passing" is checked instead of asserted.

The 85% coverage figure in all three tasks was rescoped to the 80% the build
enforces (#108). #17 lost its entity, SQL and endpoint listings: the code and
the OpenAPI document hold them.
