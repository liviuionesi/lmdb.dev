<!-- number: 298
     state: CLOSED
     labels: P2-medium, task
     milestone: Sprint 12
     parent: #295
     children: none -->
# [TASK] Audit story #124 and its 1 tasks

## Task
Audit story #124 and everything under it with `/audit-artifacts 124`:
shape, voice, truth, proof.

Subject: the frontend build to be audit-clean and CI-green

Scope: Story #124 and its 1 tasks.

## Acceptance Criteria
- [x] Task #131 audited: matched the task template and writing standard;
      2 of 3 criteria named no proof, fixed with command/file references;
      the stale "2 skipped `media.test.js` tests" note was superseded by
      #132's fix, corrected; a live `npm audit` regression found and
      tracked in #327
- [x] Story #124 audited: matched the user-story template and writing
      standard; 3 of 4 criteria named no proof, fixed with command
      references; criteria now reflect what #131 and current verification
      actually prove, including the #327 regression
- [x] Metadata and linkage correct across story #124 and its task: one
      type label, one priority label, no `sprint-N` label, native parent
      link present on both — confirmed via `issue_read`, no changes needed

## Estimate
**Hours:** 2

## Notes
Parent story #295 covers epic #121 as a whole. The epic body
itself is audited there, not here.

`audit-check.py`'s mechanical checks were applied by hand (`gh` is not
available in this environment); every finding was verified by running
`npm audit`, `npm test -- --coverage`, `npm run lint` and `npm run build`
in `frontend/lmdb`, and by reading `media.test.js`, `frontend-ci.yml` and
`vite.config.mjs` directly.