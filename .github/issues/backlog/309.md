<!-- number: 309
     state: CLOSED
     labels: P2-medium, task
     milestone: Sprint 14
     parent: #305
     children: none -->
# [TASK] Audit story #199 and its 4 tasks

## Task
Audit story #199 and everything under it with `/audit-artifacts 199`:
shape, voice, truth, proof.

Subject: the search bar to highlight the words driving my query as I type

Scope: Story #199 and its 4 tasks.

## Acceptance Criteria
- [x] Task #207 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Task #208 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Task #209 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Task #210 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Story #199 audited: matches the user-story template, meets the writing
      standard, and its criteria reflect what the audited tasks actually prove
- [x] Metadata and linkage correct across story #199 and its tasks: one type
      label, one priority label, no `sprint-N` label, native parent link present

## Estimate
**Hours:** 5

## Notes
Parent story #305 covers epic #195 as a whole. The epic body
itself is audited there, not here.

Findings: #207 had an invented `## Implementation` section (renamed to `## Notes`) and 4 criteria with no named test. #210 had no `## Notes` section and a narrated-investigation-log style AC2. #199 had "actually" (filler) in AC5 and a Definition of Done line padded with test counts and coverage percentages (cut per `VOICE.md`). All 5 issues' acceptance criteria now name a real, currently-passing test: backend tests verified against source in `AiServiceIntegrationTest`/`QuerySpanExtractorTest` (JDK 25 toolchain unavailable in this sandbox, so not re-run here; `backend-ci.yml` runs them), frontend tests re-run live (`npx vitest run` on `Search.test.jsx`, `QueryHighlightOverlay.test.jsx`, `HighlightLegend.test.jsx`, `currentGenreOrCategory.test.js` — 70/70 pass).
