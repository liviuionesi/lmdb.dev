<!-- number: 310
     state: CLOSED
     labels: P2-medium, task
     milestone: Sprint 14
     parent: #305
     children: none -->
# [TASK] Audit story #200 and its 7 tasks

## Task
Audit story #200 and everything under it with `/audit-artifacts 200`:
shape, voice, truth, proof.

Subject: voice control to reliably understand English and German, including regional accents and dialects

Scope: Story #200 and its 7 tasks.

## Acceptance Criteria
- [x] Task #211 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Task #212 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Task #213 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Task #214 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Task #215 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Task #216 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Task #229 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Story #200 audited: matches the user-story template, meets the writing
      standard, and its criteria reflect what the audited tasks actually prove
- [x] Metadata and linkage correct across story #200 and its tasks: one type
      label, one priority label, no `sprint-N` label, native parent link present

## Estimate
**Hours:** 8

## Notes
Parent story #305 covers epic #195 as a whole. The epic body
itself is audited there, not here.

Findings: #229's acceptance criteria named a script variable (`VOSK_MODEL_SHA256`) that doesn't exist in `download-vosk-model.sh` (the real variable is `MODEL_SHA256[en]`/`MODEL_SHA256[de]`) — corrected. #215's Notes were a narrated investigation log (test counts, "compiles clean" detail); trimmed to the decision and the remaining gap. #200's own acceptance criteria were all unchecked; 4 of 5 are proven by #212/#213/#214's tests and are now checked, the 5th (accent/dialect recognition) stays open on #215's real block (no sandbox has run the fixtures against real Vosk models). #215 and #229 stay open — both are genuinely blocked (no network egress to `alphacephei.com` in this sandbox, confirmed again this session), not stalled on missing work.
