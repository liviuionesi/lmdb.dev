<!-- number: 292
     state: CLOSED
     labels: P2-medium, task
     milestone: Sprint 12
     parent: #287
     children: none -->
# [TASK] Audit story #236 and its 2 tasks

## Task
Audit story #236 and everything under it with `/audit-artifacts 236`:
shape, voice, truth, proof.

Subject: the gateway's runtime security-management API restricted to administrators

Scope: Story #236 and its 2 tasks.

## Acceptance Criteria
- [x] Task #237 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Task #238 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Story #236 audited: matches the user-story template, meets the writing
      standard, and its criteria reflect what the audited tasks actually prove
- [x] Metadata and linkage correct across story #236 and its tasks: one type
      label, one priority label, no `sprint-N` label, native parent link present

## Estimate
**Hours:** 3

## Notes
Parent story #287 covers epic #76 as a whole. The epic body
itself is audited there, not here.

#237 already matched the template and named real proof (`GatewayIntegrationTest`); no change needed. #236 and #238 were missing proof on their criteria: #236's four criteria now name the `GatewayIntegrationTest` methods that prove them, and #238's Notes were cut from a run log to the two facts that matter (blocking env, path to closing). #238 stays open: `:backend:user-service:test` still needs a run with both a JDK 25 toolchain and Docker network access to Postgres, neither available in this or the prior sandbox.