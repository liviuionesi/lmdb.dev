<!-- number: 284
     state: CLOSED
     labels: P2-medium, task
     milestone: Sprint 11
     parent: #281
     children: none -->
# [TASK] Audit story #98 and its 2 tasks

## Task
Audit story #98 and everything under it with `/audit-artifacts 98`: shape, voice, truth, proof.

Subject: facade cache hardening and load-tested SLOs

Scope: Story #98 and its 2 tasks.

## Acceptance Criteria
- [x] Task #44 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Task #45 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Story #98 audited: matches the user-story template, meets the writing standard, and its criteria reflect what the audited tasks actually prove
- [x] Metadata and linkage correct across story #98 and its tasks: one type label, one priority label, no `sprint-N` label, native parent link present

## Estimate
**Hours:** 3

## Notes
Parent story #281 covers epic #39 as a whole. The epic body itself is audited there, not here.

Findings: the commit hashes `cfbda14` (#44) and `1e6706c` (#45) predate a repository history rewrite and no longer resolve, replaced with the test/class names that are the current record. #45's "Gatling HTML report committed under docs/" criterion was false — the report is gitignored and regenerated per run — rescoped to state that. Story #98's SLO criterion was rescoped to say the test asserts the report is generated, not the SLO values themselves. Verification was by reading the tests and implementation, not by running `:backend:movie-service:test`: this sandbox has no Java 25 toolchain and Testcontainers' `mongo:8.0` pull is blocked by network policy (same limitation #250 and #238 document).
