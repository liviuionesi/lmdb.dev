<!-- number: 258
     state: CLOSED
     labels: P2-medium, task
     milestone: Sprint 9
     parent: #270
     children: none -->
# [TASK] Audit Story #86 and Tasks #19-#20 (integration testing and static-analysis quality gates)

## Task
Audit story #86 and everything under it with `/audit-artifacts 86`:
shape, voice, truth, proof.

Subject: integration tests and static-analysis quality gates across the core services

Scope: Story #86 and its 2 tasks.

## Acceptance Criteria
- [x] Task #19 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Task #20 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Story #86 audited: matches the user-story template, meets the writing
      standard, and its criteria reflect what the audited tasks actually prove
- [x] Metadata and linkage correct across story #86 and its tasks: one type
      label, one priority label, no `sprint-N` label, native parent link present
      (`audit-check.py 86 --tree`)

## Estimate
**Hours:** 3

## Notes
Parent story #270 covers epic #15 as a whole. The epic body
itself is audited there, not here.

#20 claimed a fix for the one real bug SonarQube found, a daylight-saving
error in `DateUtils.hoursBetween`, with no test holding it.
`DateUtilsTest.hoursBetween_shouldCountRealElapsedHoursAcrossDaylightSaving`
now does.

Two stale facts corrected: the quality profile is
`infrastructure/sonarqube/lmdb-way-java-profile.xml`, not `filmpire-way-…`,
and the analysis runs against SonarCloud on `main`, not a local Community
server. Test and coverage counts were removed from the criteria in favour of
the suites and gates that produce them.
