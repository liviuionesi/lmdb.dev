<!-- number: 280
     state: CLOSED
     labels: P2-medium, task
     milestone: Sprint 11
     parent: #276
     children: none -->
# [TASK] Audit story #115 and its 2 tasks

## Task
Audit story #115 and everything under it with `/audit-artifacts 115`:
shape, voice, truth, proof.

Subject: to upload custom profile avatars and review attachments so that I can personalize my identity and reactions on the platform

Scope: Story #115 and its 2 tasks.

## Acceptance Criteria
- [x] Task #116 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Task #117 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Story #115 audited: matches the user-story template, meets the writing
      standard, and its criteria reflect what the audited tasks actually prove
- [x] Metadata and linkage correct across story #115 and its tasks: one type
      label, one priority label, no `sprint-N` label, native parent link present

## Estimate
**Hours:** 3

## Notes
Parent story #276 covers epic #35 as a whole. The epic body
itself is audited there, not here.

Verified live: installed `frontend/lmdb` deps and ran the 4 test files #116/#117 name (`Profile.test.jsx`, `NavBar.test.jsx`, `ReviewMediaSection.test.jsx`, `MovieInformation.test.jsx`) — 33/33 pass. Confirmed `backend/media-service`'s `EntityType`/`MediaType` enums and `MediaController` support the `USER`/`AVATAR` and `MOVIE_REVIEW` values both tasks claim. #115's body used unbolded `As a/I want/So that` and a stray `Milestone: unassigned` line restating a GitHub field; both fixed. #116's Scope named the retired `frontend/filmpire` path; corrected to `frontend/lmdb`. #116/#117's Notes said "Jest tests"; this repo runs Vitest, not Jest — corrected. Added proof references (test names) to every criterion that had none.
