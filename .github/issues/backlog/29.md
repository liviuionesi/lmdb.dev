<!-- number: 29
     state: CLOSED
     labels: task, backend
     milestone: Sprint 5 -->
# [TASK] Close validation gaps from closed issues #4, #5, #14

## Task
Close validation gaps found in a 2026-07-21 audit of issues previously closed as done: #4 (GitHub setup), #5 (Docker Compose), #14 (shared library).

## Parent Story
Parent: #89

## Acceptance Criteria
- [x] #5: added `docker-compose.prod.yml` (resource-limited override); dev-only UIs (Adminer/Mongo Express/Redis Commander) moved behind a `dev-tools` compose profile; added `infrastructure/scripts/logs.sh`; renamed `env.example` -> `.env.example`
- [x] #14: deleted `@LogExecutionTime`/`@ValidateRequest`/`@CacheResult`/`@RateLimited` — bare unused annotations with no aspect implementation, fully redundant with Spring's own `@Cacheable`, Bucket4j rate limiting, and `@Valid`/`@Validated`
- [x] #14: added tests for all 6 exception classes + `ErrorResponse` + `HalResource` — shared-library suite grew from ~40 to 119 tests
- [x] #4: documented the decision to keep branch protection off (solo, direct-to-main workflow) in ARCHITECTURE.md §9.3, instead of leaving the stale claim that it was enabled
- [x] #4: `project-automation.yml` CI failure descoped to #28, not fixed here

## Estimate
**Hours:** 6

## Notes
#12 (config service Git backend) was reopened separately; #16 (movie-service failing tests) was fixed in commit `ca87c10` — neither is this issue's scope.

Reference: ARCHITECTURE.md

