<!-- number: 317
     state: CLOSED
     labels: P2-medium, task
     milestone: Sprint 15
     parent: #316
     children: none -->
# [TASK] Audit the 7 audit tooling tasks

## Task
Audit the 7 audit tooling tasks with `/audit-artifacts`: shape, voice, truth, proof.

Written during epic #251 itself: the /work-issue and /audit-artifacts skills, issue-history.sh, the git mirror, the Antigravity parity work, and the hierarchy de-duplication. #262 is closed as superseded and is audited as such.

Scope: #227, #262, #263, #264, #265, #266, #267.

## Acceptance Criteria
- [x] Task #227 audited: matched the template and writing standard; two
      criteria named no proof, fixed with references to
      `.claude/skills/work-issue/SKILL.md`; its `## No parent` reason was
      inside an HTML comment (invisible on GitHub), replaced with plain
      text; the no-parent reason itself is real
- [x] Task #262 audited: matches the task template, meets the writing
      standard, its one criterion is proven by the four story references
      it names (#268-#271), and its no-parent reason is real
- [x] Task #263 audited: matched the template and writing standard; its
      five criteria named specific past revision/file counts that go stale
      as the board changes, rewritten as re-checkable capability claims
      with the counts moved to Notes as a one-time verification; every
      claim checked against `infrastructure/scripts/issue-history.sh`'s
      code; its no-parent reason is real
- [x] Task #264 audited: matched the template and writing standard; two
      criteria named no proof, merged into one `(manual: ...)` criterion
      (5 → 4 criteria, one manual as the cap allows); its no-parent reason
      is real
- [x] Task #265 audited: matched the template and writing standard; one
      criterion named no proof, fixed with a reference to
      `.claude/skills/audit-artifacts/SKILL.md`'s Parallelism section;
      every claim re-checked against `AGENTS.md` and the skill file; its
      no-parent reason is real
- [x] Task #266 audited: matched the template and writing standard; one
      criterion named no proof, fixed with a reference to
      `.claude/hooks/graphify-guard.sh`; every claim re-checked against
      `AGENTS.md`, the three skill files and the hook script; its
      no-parent reason is real
- [x] Task #267 audited: matched the template and writing standard; three
      criteria named no proof or a stale point-in-time count, fixed with
      template/file references and a repeatable `grep`; one claim in the
      Task body ("the parent line stays") is false today — #253 removed
      that line — rescoped in place and recorded in Notes; its no-parent
      reason is real

## Estimate
**Hours:** 8

## Notes
None of these has a parent story. The audit confirmed that is still the
right call for each one.

`audit-check.py`'s mechanical checks were applied by hand (`gh` is not
available in this environment); every finding above was verified by
reading the referenced source file or template directly.
