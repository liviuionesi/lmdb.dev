<!-- number: 259
     state: CLOSED
     labels: P2-medium, task
     milestone: Sprint 10
     parent: #271
     children: none -->
# [TASK] Audit Story #87 and Tasks #23-#24 (structured logging, metrics, local observability stack)

## Task
Audit story #87 and everything under it with `/audit-artifacts 87`:
shape, voice, truth, proof.

Subject: structured logging and metrics plus a local observability stack

Scope: Story #87 and its 2 tasks.

## Acceptance Criteria
- [x] Task #23 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Task #24 audited: matches the task template, meets the writing standard, every claim checked against the code, every criterion names its proof
- [x] Story #87 audited: matches the user-story template, meets the writing
      standard, and its criteria reflect what the audited tasks actually prove
- [x] Metadata and linkage correct across story #87 and its tasks: one type
      label, one priority label, no `sprint-N` label, native parent link present

## Estimate
**Hours:** 3

## Notes
Parent story #271 covers epic #22 as a whole. The epic body
itself is audited there, not here.

Findings: #23's "376 tests, 0 failures" criterion was a stale count with no repeatable proof — rewritten to name `PrometheusEndpointTest` and a new `StructuredLoggingTest` (JSON-logging had no test before this audit). #24's index-name criterion said `filmpire-logs`; the actual pipeline and index template use `lmdb-logs` — corrected. #24's two independent "verified live" criteria (Kibana, Grafana/Alertmanager) were merged into one manual criterion to respect the one-manual-per-issue cap.
