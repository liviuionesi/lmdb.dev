# Copy to terraform.tfvars (gitignored) before running `terraform apply`.
# See ../README.md for the full bootstrap + apply/destroy walkthrough.

alert_emails = ["liviuionesi@gmail.com"]

# Contents of your SSH public key, e.g.:
#   ssh_public_key = "$(cat ~/.ssh/id_ed25519.pub)"
ssh_public_key = "REPLACE_ME"

# Uncomment to override defaults (aws/variables.tf):
# region        = "us-west-2"
# instance_type = "t3.small"  # default; t3.micro OOM-thrashes (#27 live findings)
