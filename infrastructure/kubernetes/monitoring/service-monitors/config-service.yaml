# ServiceMonitor for config-service (issue #24, ARCHITECTURE.md section 12.1).
# See README.md for the labeling contract this depends on.
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: config-service-monitor
  namespace: monitoring
  labels:
    app: config-service
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: config-service
      monitoring: enabled
  endpoints:
    - targetPort: 8888
      path: /actuator/prometheus
      interval: 10s
