# ServiceMonitor for discovery-service (issue #24, ARCHITECTURE.md section 12.1).
# See README.md for the labeling contract this depends on.
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: discovery-service-monitor
  namespace: monitoring
  labels:
    app: discovery-service
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: discovery-service
      monitoring: enabled
  endpoints:
    - targetPort: 8761
      path: /actuator/prometheus
      interval: 10s
