apiVersion: v1
kind: Service
metadata:
  name: actor-service
  labels:
    app: actor-service
    monitoring: enabled
spec:
  type: ClusterIP
  selector:
    app: actor-service
  ports:
    - name: http
      port: 8083
      targetPort: 8083
